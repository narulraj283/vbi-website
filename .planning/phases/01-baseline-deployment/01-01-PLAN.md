---
phase: 01-baseline-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/narenarulrajah/AnjaleeClaude/VBI/02-Website/vbi-repo/ (109 modified HTML files + CNAME)
autonomous: false
requirements: [DEPL-01, DEPL-03]
user_setup:
  - service: GoDaddy DNS
    why: "CNAME record for vbi subdomain is missing or broken (NXDOMAIN confirmed 2026-03-15)"
    dashboard_config:
      - task: "Add or fix CNAME record: Host = vbi, Points to = narulraj283.github.io, TTL = default"
        location: "GoDaddy DNS Management for narenlife.com"

must_haves:
  truths:
    - "109 QA-fixed files are committed and pushed to narulraj283/vbi-website on GitHub"
    - "CNAME file containing vbi.narenlife.com is present in the pushed commit"
    - "DNS resolves vbi.narenlife.com to narulraj283.github.io (no NXDOMAIN)"
  artifacts:
    - path: "CNAME"
      provides: "Custom domain binding for GitHub Pages"
      contains: "vbi.narenlife.com"
    - path: "index.html"
      provides: "Homepage with QA fixes applied"
      contains: "rel=\"noopener\""
  key_links:
    - from: "GoDaddy CNAME record"
      to: "narulraj283.github.io"
      via: "DNS resolution"
      pattern: "nslookup vbi.narenlife.com returns narulraj283.github.io"
    - from: "Local repo commit"
      to: "GitHub Pages deployment"
      via: "git push origin main"
      pattern: "git log --oneline -1 on remote matches local"
---

<objective>
Fix broken DNS and push all 109 QA-fixed files to GitHub Pages, preserving the CNAME file for the custom domain.

Purpose: Establish the deployment baseline -- all QA fixes live on GitHub, DNS resolving correctly, ready for HTTPS provisioning in Plan 02.
Output: 109 QA-fixed pages deployed to GitHub Pages with working DNS for vbi.narenlife.com.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-deployment/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Fix GoDaddy DNS CNAME record for vbi.narenlife.com</name>
  <read_first>
    - /Users/narenarulrajah/AnjaleeClaude/VBI2Code/.planning/phases/01-baseline-deployment/01-RESEARCH.md (Pitfall 2 section for DNS details)
  </read_first>
  <action>
The DNS for vbi.narenlife.com currently returns NXDOMAIN (confirmed 2026-03-15). The user must log into GoDaddy DNS management for narenlife.com and add or fix a CNAME record with these exact values:

- Type: CNAME
- Host: vbi
- Points to: narulraj283.github.io
- TTL: Default (1 hour or 600 seconds)

After saving, DNS propagation takes 5-60 minutes. The executor should verify propagation with:

```bash
nslookup vbi.narenlife.com
```

Expected result: Non-authoritative answer showing vbi.narenlife.com is an alias for narulraj283.github.io (NOT NXDOMAIN or SERVFAIL).

If nslookup still shows NXDOMAIN after 60 minutes, check for:
- Conflicting A records for the "vbi" subdomain at GoDaddy (delete them)
- Typos in the CNAME target (must be exactly narulraj283.github.io with no trailing dot unless GoDaddy adds it automatically)
  </action>
  <verify>
    <automated>nslookup vbi.narenlife.com 2>&1 | grep -i "narulraj283.github.io\|canonical name"</automated>
  </verify>
  <acceptance_criteria>
    - `nslookup vbi.narenlife.com` output contains "narulraj283.github.io" or "canonical name"
    - `nslookup vbi.narenlife.com` output does NOT contain "NXDOMAIN" or "SERVFAIL"
  </acceptance_criteria>
  <done>DNS resolves vbi.narenlife.com to narulraj283.github.io -- NXDOMAIN is gone</done>
  <resume-signal>Type "dns fixed" after saving the CNAME record and confirming nslookup works</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Commit and push 109 QA-fixed files to GitHub</name>
  <read_first>
    - /Users/narenarulrajah/AnjaleeClaude/VBI/02-Website/vbi-repo/CNAME (verify content is exactly "vbi.narenlife.com")
    - /Users/narenarulrajah/AnjaleeClaude/VBI/02-Website/vbi-repo/index.html (verify QA fixes are present -- look for rel="noopener" and canonical URL)
  </read_first>
  <files>/Users/narenarulrajah/AnjaleeClaude/VBI/02-Website/vbi-repo/ (all 109 modified files)</files>
  <action>
In the VBI repo at /Users/narenarulrajah/AnjaleeClaude/VBI/02-Website/vbi-repo/:

1. Verify CNAME file content is exactly "vbi.narenlife.com" (one line, no trailing newline issues):
   ```bash
   cat /Users/narenarulrajah/AnjaleeClaude/VBI/02-Website/vbi-repo/CNAME
   ```

2. Stage all modified files:
   ```bash
   cd /Users/narenarulrajah/AnjaleeClaude/VBI/02-Website/vbi-repo && git add -A
   ```

3. Verify CNAME is NOT listed as deleted in staged changes:
   ```bash
   git diff --cached --name-status | grep CNAME
   ```
   Expected: empty output (CNAME is already tracked and unchanged) or "M CNAME" (modified). Must NOT show "D CNAME".

4. Verify the staged file count is approximately 109:
   ```bash
   git diff --cached --stat | tail -1
   ```

5. Commit with descriptive message:
   ```bash
   git commit -m "$(cat <<'EOF'
Apply QA fixes across 109 pages

- Fix canonical URLs to use https://vbi.narenlife.com
- Add rel=noopener to all external links
- Fix navigation links across all pages
- Add favicon reference to all pages
- Update meta tags for SEO
EOF
)"
   ```

6. Push to origin main:
   ```bash
   git push origin main
   ```

If git push fails with authentication error (403 or credential prompt), this becomes a checkpoint -- the user will need to either:
- Confirm macOS Keychain has valid GitHub credentials, OR
- Create a fine-grained PAT at https://github.com/settings/tokens with "Contents: Read and write" permission scoped to the narulraj283/vbi-website repo only, then retry the push.
  </action>
  <verify>
    <automated>cd /Users/narenarulrajah/AnjaleeClaude/VBI/02-Website/vbi-repo && git log --oneline -1 && git show HEAD:CNAME && echo "---PUSH CHECK---" && git status --short | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` shows a commit message containing "QA fixes"
    - `git show HEAD:CNAME` outputs exactly "vbi.narenlife.com"
    - `git status --short | wc -l` outputs "0" (clean working tree after commit)
    - `git push` completed without error (exit code 0)
  </acceptance_criteria>
  <done>All 109 QA-fixed files and CNAME are committed and pushed to GitHub. Working tree is clean. GitHub Pages rebuild is triggered.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. DNS resolves: `nslookup vbi.narenlife.com` shows narulraj283.github.io
2. Push succeeded: `cd /Users/narenarulrajah/AnjaleeClaude/VBI/02-Website/vbi-repo && git log origin/main --oneline -1` matches local HEAD
3. CNAME preserved: `git show HEAD:CNAME` outputs "vbi.narenlife.com"
4. Clean state: `git status --short` shows no changes
</verification>

<success_criteria>
- DNS for vbi.narenlife.com resolves to narulraj283.github.io (no NXDOMAIN)
- 109 QA-fixed files are pushed to the main branch on GitHub
- CNAME file is present in the commit with content "vbi.narenlife.com"
- GitHub Pages rebuild triggered (site accessible at vbi.narenlife.com within minutes)
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-deployment/01-01-SUMMARY.md`
</output>
