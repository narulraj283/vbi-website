---
phase: 08-events-structured-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [events/index.html]
autonomous: true
requirements: [SCHM-01]

must_haves:
  truths:
    - "The events page head contains a script tag with type application/ld+json"
    - "Each of the 7 events on the page has a corresponding Event object in the JSON-LD"
    - "Every Event object includes at minimum name, startDate, and location"
    - "The JSON-LD is valid JSON and valid schema.org markup"
  artifacts:
    - path: "events/index.html"
      provides: "Events page with schema.org Event JSON-LD structured data"
      contains: "application/ld+json"
  key_links:
    - from: "events/index.html JSON-LD"
      to: "events/index.html visible content"
      via: "Event data matches what users see on the page"
      pattern: "application/ld\\+json"
---

<objective>
Add schema.org Event JSON-LD structured data to the VBI events page so it is eligible for Google rich results.

Purpose: Search engines use Event structured data to display rich snippets (date, location, registration links) in search results, driving more traffic to VBI events.
Output: Updated events/index.html with a valid JSON-LD script block containing all 7 events.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@events/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject schema.org Event JSON-LD into events page head</name>
  <files>events/index.html</files>
  <read_first>events/index.html</read_first>
  <action>
Insert a single `<script type="application/ld+json">` block inside the `<head>` element of events/index.html, immediately before the closing `</head>` tag. The JSON-LD must contain an array of 7 schema.org Event objects matching the events visible on the page.

Each Event object must use `@context: "https://schema.org"` and `@type: "Event"` and include these properties:

**Event 1 — VBI Annual Summit 2026:**
- name: "VBI Annual Summit 2026"
- startDate: "2026-10-15"
- endDate: "2026-10-17"
- eventAttendanceMode: "https://schema.org/OfflineEventAttendanceMode"
- eventStatus: "https://schema.org/EventScheduled"
- location: { @type: "Place", name: "San Diego, California", address: { @type: "PostalAddress", addressLocality: "San Diego", addressRegion: "CA", addressCountry: "US" } }
- description: "Join the largest gathering of independent veterinary practice owners and managers in the country. Three days of keynote sessions, breakout workshops, peer networking, and an exclusive partner showcase."
- organizer: { @type: "Organization", name: "Veterinary Business Institute", url: "https://vbi.narenlife.com" }

**Event 2 — Reducing No-Shows by 30%:**
- name: "Reducing No-Shows by 30%"
- startDate: "2026-04-15T14:00:00-04:00"
- endDate: "2026-04-15T15:30:00-04:00"
- eventAttendanceMode: "https://schema.org/OnlineEventAttendanceMode"
- eventStatus: "https://schema.org/EventScheduled"
- location: { @type: "VirtualLocation", url: "https://vbi.narenlife.com/events/" }
- description: "Discover proven strategies to improve patient attendance, reduce cancellations, and increase your bottom line."
- organizer: { @type: "Organization", name: "Veterinary Business Institute", url: "https://vbi.narenlife.com" }

**Event 3 — Partner Demo Day Q2:**
- name: "Partner Demo Day Q2"
- startDate: "2026-05-08T13:00:00-04:00"
- endDate: "2026-05-08T15:00:00-04:00"
- eventAttendanceMode: "https://schema.org/OnlineEventAttendanceMode"
- eventStatus: "https://schema.org/EventScheduled"
- location: { @type: "VirtualLocation", url: "https://vbi.narenlife.com/events/" }
- description: "Discover the latest innovations from our top partners. See live demonstrations of practice management software, marketing tools, staffing solutions, and more."
- organizer: { @type: "Organization", name: "Veterinary Business Institute", url: "https://vbi.narenlife.com" }

**Event 4 — Reading Your P&L Like a CFO:**
- name: "Reading Your P&L Like a CFO"
- startDate: "2026-05-20"
- eventAttendanceMode: "https://schema.org/OnlineEventAttendanceMode"
- eventStatus: "https://schema.org/EventScheduled"
- location: { @type: "VirtualLocation", url: "https://vbi.narenlife.com/events/" }
- description: "Master the financial statements that matter most. Learn to analyze profit and loss reports, identify cost drivers, spot opportunities, and make data-driven decisions about your practice."
- organizer: { @type: "Organization", name: "Veterinary Business Institute", url: "https://vbi.narenlife.com" }

**Event 5 — Regional Meetup: Southeast:**
- name: "Regional Meetup: Southeast"
- startDate: "2026-06-05T18:00:00-04:00"
- endDate: "2026-06-05T20:00:00-04:00"
- eventAttendanceMode: "https://schema.org/OfflineEventAttendanceMode"
- eventStatus: "https://schema.org/EventScheduled"
- location: { @type: "Place", name: "Atlanta, Georgia", address: { @type: "PostalAddress", addressLocality: "Atlanta", addressRegion: "GA", addressCountry: "US" } }
- description: "Connect face-to-face with fellow practice owners in your region. Share challenges, celebrate wins, discuss local market dynamics, and build relationships with peers."
- organizer: { @type: "Organization", name: "Veterinary Business Institute", url: "https://vbi.narenlife.com" }

**Event 6 — Building a Hiring System:**
- name: "Building a Hiring System"
- startDate: "2026-06-17T14:00:00-04:00"
- endDate: "2026-06-17T15:30:00-04:00"
- eventAttendanceMode: "https://schema.org/OnlineEventAttendanceMode"
- eventStatus: "https://schema.org/EventScheduled"
- location: { @type: "VirtualLocation", url: "https://vbi.narenlife.com/events/" }
- description: "Solve your staffing challenges with a systematic approach to recruitment, onboarding, and retention."
- organizer: { @type: "Organization", name: "Veterinary Business Institute", url: "https://vbi.narenlife.com" }

**Event 7 — Partner Demo Day Q3:**
- name: "Partner Demo Day Q3"
- startDate: "2026-08-12T13:00:00-04:00"
- endDate: "2026-08-12T15:00:00-04:00"
- eventAttendanceMode: "https://schema.org/OnlineEventAttendanceMode"
- eventStatus: "https://schema.org/EventScheduled"
- location: { @type: "VirtualLocation", url: "https://vbi.narenlife.com/events/" }
- description: "The summer edition of our Partner Demo Day series. Explore the latest innovations in practice technology, staffing solutions, marketing tools, and business services."
- organizer: { @type: "Organization", name: "Veterinary Business Institute", url: "https://vbi.narenlife.com" }

Format the JSON with 2-space indentation for readability. Do NOT use an @graph wrapper — use a plain JSON array of Event objects, each with its own @context and @type. This is valid and the simplest approach for Google.

Do NOT modify any other part of the HTML file. Only insert the script tag.
  </action>
  <acceptance_criteria>
- grep -c "application/ld+json" events/index.html returns 1
- grep -c '"@type": "Event"' events/index.html returns 7 (one per event, exact match with quotes)
- grep -c '"startDate"' events/index.html returns 7
- grep -c '"location"' events/index.html returns 7
- grep -c '"name"' events/index.html returns at least 7
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/narenarulrajah/AnjaleeClaude/VBI2Code && echo "ld+json tags: $(grep -c 'application/ld+json' events/index.html)" && echo "Event types: $(grep -c '"@type": "Event"' events/index.html)" && echo "startDate: $(grep -c '"startDate"' events/index.html)" && echo "location: $(grep -c '"location"' events/index.html)" && python3 -c "import json; f=open('events/index.html'); html=f.read(); f.close(); start=html.index('application/ld+json'); start=html.index('[',start); end=html.index('</script>',start); data=json.loads(html[start:end]); print(f'Valid JSON: {len(data)} events'); assert len(data)==7, f'Expected 7 events, got {len(data)}'; [assert all(k in e for k in ['name','startDate','location']) for e in data]; print('All events have required fields')"</automated>
  </verify>
  <done>
- events/index.html contains exactly one application/ld+json script block in the head
- The JSON-LD contains 7 Event objects matching the 7 visible events
- Each Event has name, startDate, and location at minimum
- The JSON is parseable (no syntax errors)
- Virtual events use VirtualLocation, in-person events use Place with PostalAddress
- All events include eventAttendanceMode, eventStatus, description, and organizer
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate JSON-LD structure and schema.org compliance</name>
  <files>events/index.html</files>
  <read_first>events/index.html</read_first>
  <action>
After Task 1 completes, validate the injected JSON-LD by:

1. Extract the JSON-LD block from events/index.html using Python
2. Parse it as JSON and verify:
   - Exactly 7 Event objects
   - Each has @context set to "https://schema.org"
   - Each has @type set to "Event"
   - Each has name (string), startDate (string), location (object)
   - Each has eventAttendanceMode and eventStatus
   - The Annual Summit has endDate "2026-10-17"
   - Virtual events have location.@type = "VirtualLocation"
   - In-person events (Summit, Meetup) have location.@type = "Place"
   - All startDate values with times include timezone offset (-04:00 for ET)
3. Verify no other HTML was modified by checking the file still contains key markers:
   - The page title is still "Events | Veterinary Business Institute"
   - The hero h1 is still "Events That Drive Real Practice Growth"
   - The footer copyright is still present
   - The main.js script tag is still present

If any validation fails, fix the JSON-LD in events/index.html to correct the issue. Do not alter any HTML outside the JSON-LD script block.
  </action>
  <acceptance_criteria>
- python3 validation script exits with code 0
- grep "Events That Drive Real Practice Growth" events/index.html returns a match (HTML not corrupted)
- grep "main.js" events/index.html returns a match (scripts intact)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/narenarulrajah/AnjaleeClaude/VBI2Code && python3 -c "
import json, sys
with open('events/index.html') as f:
    html = f.read()

# Check HTML integrity
assert 'Events That Drive Real Practice Growth' in html, 'Hero heading missing'
assert 'main.js' in html, 'main.js script missing'
assert html.count('application/ld+json') == 1, 'Expected exactly 1 ld+json block'

# Extract and parse JSON-LD
start = html.index('application/ld+json')
start = html.index('[', start)
end = html.index('</script>', start)
events = json.loads(html[start:end])

assert len(events) == 7, f'Expected 7 events, got {len(events)}'

for i, e in enumerate(events):
    assert e.get('@context') == 'https://schema.org', f'Event {i}: missing @context'
    assert e.get('@type') == 'Event', f'Event {i}: missing @type'
    assert 'name' in e, f'Event {i}: missing name'
    assert 'startDate' in e, f'Event {i}: missing startDate'
    assert 'location' in e, f'Event {i}: missing location'
    assert 'eventAttendanceMode' in e, f'Event {i}: missing eventAttendanceMode'
    assert 'eventStatus' in e, f'Event {i}: missing eventStatus'
    assert 'organizer' in e, f'Event {i}: missing organizer'

# Check specific events
names = [e['name'] for e in events]
assert 'VBI Annual Summit 2026' in names, 'Summit missing'
assert 'Regional Meetup: Southeast' in names, 'Meetup missing'

# Check virtual vs in-person
for e in events:
    if 'OnlineEventAttendanceMode' in e['eventAttendanceMode']:
        assert e['location']['@type'] == 'VirtualLocation', f'{e[\"name\"]}: virtual should use VirtualLocation'
    else:
        assert e['location']['@type'] == 'Place', f'{e[\"name\"]}: in-person should use Place'

print('ALL VALIDATIONS PASSED - 7 events with valid schema.org Event JSON-LD')
"</automated>
  </verify>
  <done>
- All 7 events pass schema.org Event validation
- Virtual events correctly use VirtualLocation, in-person use Place
- HTML page integrity confirmed (no corruption from JSON-LD injection)
- JSON-LD is syntactically valid and parseable
  </done>
</task>

</tasks>

<verification>
1. events/index.html contains exactly one `<script type="application/ld+json">` block
2. The JSON-LD contains 7 schema.org Event objects
3. Each Event has name, startDate, location, eventAttendanceMode, eventStatus, description, organizer
4. The rest of the HTML is unchanged
5. The JSON is valid (parseable by Python json module)
</verification>

<success_criteria>
- The events page at events/index.html has valid schema.org Event JSON-LD in the head
- All 7 visible events are represented in the structured data
- Each Event includes at minimum: name, startDate, location (satisfying SCHM-01)
- The page renders identically before and after (no visible changes, only head metadata added)
</success_criteria>

<output>
After completion, create `.planning/phases/08-events-structured-data/08-01-SUMMARY.md`
</output>
